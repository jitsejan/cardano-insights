name: 'dbt_github'
version: '1.0.0'
profile: 'tech_intel'
config-version: 2

model-paths: ['models']
target-path: 'target'
clean-targets: ['target']

models:
  dbt_github:
    bronze:
      +materialized: table
      +docs:
        node_color: "#8B4513"
    silver:
      +materialized: table
      +docs:
        node_color: "#C0C0C0"
    gold:
      +materialized: table
      +docs:
        node_color: "#FFD700"